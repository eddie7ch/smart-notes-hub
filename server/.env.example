# Required: used for embeddings, and as chat provider when AI_CHAT_PROVIDER=openai
OPENAI_API_KEY=

# Optional: used as chat provider when AI_CHAT_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# openai | anthropic (defaults to openai). Embeddings always use OpenAI.
AI_CHAT_PROVIDER=openai

PORT=8080
